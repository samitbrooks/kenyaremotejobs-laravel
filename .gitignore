*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# public/build is deliberately tracked, not ignored — this is a
# Node-free shared host with no CI, so committing the compiled assets
# is how they actually get deployed: `npm run build` locally, commit,
# `git pull` on the server. Rebuild and commit whenever CSS/JS changes.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
